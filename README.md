# Habit Tracker - 习惯养成与激励系统

![image-20260116000752074](https://gitee.com/suxuefenxi/pic/raw/master/20260116000759267.png)

一个功能完善的习惯养成与激励系统，帮助用户建立良好习惯，通过打卡、积分和成就系统提升用户参与度。

## 📋 项目简介

Habit Tracker 是一个基于 Go 语言开发的 Web 应用，提供习惯管理、每日打卡、积分系统、成就解锁和排行榜等功能。用户可以创建自定义习惯，设置打卡频率，通过持续打卡获得积分和成就徽章，并在排行榜上与其他用户竞争。

## ✨ 核心功能

### 1. 用户系统
- 用户注册与登录
- JWT 身份认证
- 个人信息管理
- 用户统计数据展示

### 2. 习惯管理
- 创建自定义习惯
- 设置习惯频率（每日/每周/自定义）
- 编辑和删除习惯
- 查看习惯列表和详情
- 习惯状态追踪

### 3. 打卡系统
- 每日习惯打卡
- 打卡历史记录
- 连续打卡天数统计
- 打卡日历视图
- 打卡提醒功能

### 4. 积分系统
- 打卡获得积分奖励
- 连续打卡额外奖励
- 积分历史记录
- 积分排行榜

### 5. 成就系统
- 多种成就徽章
- 成就解锁条件
- 成就进度追踪
- 成就展示墙

### 6. 排行榜
- 全局积分排行
- 周榜/月榜/总榜
- 用户排名展示
- 排名变化趋势

### 7. 数据统计
- 个人习惯完成率
- 打卡趋势图表(todo)
- 习惯坚持天数
- 积分增长曲线(todo)

## 🖼️ 应用截图

### 首页

![image-20260116000902960](https://gitee.com/suxuefenxi/pic/raw/master/20260116000903174.png) 

### 习惯列表
![image-20260116000925343](https://gitee.com/suxuefenxi/pic/raw/master/20260116000925463.png)

### 成就系统
![image-20260116000955459](https://gitee.com/suxuefenxi/pic/raw/master/20260116000955580.png)



### 排行榜

![image-20260116001017305](https://gitee.com/suxuefenxi/pic/raw/master/20260116001017392.png)

## 🚀 快速开始

### 环境要求

- Go 1.21+
- OpenGauss 数据库
- Docker & Docker Compose（可选）

### 本地开发

1. **克隆项目**
```bash
git clone <repository-url>
cd habit-tracker
```

2. **配置环境变量**
```bash
# Windows
set DB_DSN=host=localhost port=5432 user=gaussdb password=your_password dbname=habit_tracker sslmode=disable
set PORT=8080
set JWT_SECRET=your_jwt_secret

# Linux/Mac
export DB_DSN="host=localhost port=5432 user=gaussdb password=your_password dbname=habit_tracker sslmode=disable"
export PORT=8080
export JWT_SECRET="your_jwt_secret"
```

3. **安装依赖**
```bash
go mod download
```

4. **运行应用**
```bash
go run cmd/server/main.go
```

5. **访问应用**
```
http://localhost:8080
```

### Docker 部署

1. **使用 Docker Compose**
```bash
docker-compose up -d
```

2. **查看日志**
```bash
docker-compose logs -f
```

3. **停止服务**
```bash
docker-compose down
```


## 📁 项目结构

```
habit-tracker/
├── cmd/
│   └── server/
│       └── main.go              # 应用入口
├── internal/
│   ├── config/                  # 配置管理
│   ├── db/                      # 数据库连接
│   ├── handler/                 # HTTP 处理器
│   ├── middleware/              # 中间件
│   ├── model/                   # 数据模型
│   ├── repository/              # 数据访问层
│   ├── router/                  # 路由配置
│   ├── service/                 # 业务逻辑层
│   └── utils/                   # 工具函数
├── web/                         # 前端静态文件
├── Dockerfile                   # Docker 构建文件
├── docker-compose.yml           # Docker Compose 配置
├── go.mod                       # Go 模块依赖
└── README.md                    # 项目说明
```

## 🔧 技术栈

### 后端
- **语言**: Go 1.21
- **Web 框架**: Gin
- **数据库**: OpenGauss
- **认证**: JWT
- **ORM**: database/sql

### 前端
- **框架**: HTML/CSS/JavaScript

## 📊 数据库设计

### 核心表结构

- `users` - 用户表
- `habits` - 习惯表
- `checkins` - 打卡记录表
- `points` - 积分记录表
- `achievements` - 成就定义表
- `user_achievements` - 用户成就表

## 🔐 API 文档

### 认证接口
- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录

### 习惯管理
- `GET /api/habits` - 获取习惯列表
- `POST /api/habits` - 创建习惯
- `GET /api/habits/:id` - 获取习惯详情
- `PUT /api/habits/:id` - 更新习惯
- `DELETE /api/habits/:id` - 删除习惯

### 打卡管理
- `POST /api/checkins` - 创建打卡
- `GET /api/checkins/habit/:id` - 获取习惯打卡记录
- `GET /api/checkins/user` - 获取用户打卡记录

### 积分与排行榜
- `GET /api/leaderboard` - 获取排行榜
- `GET /api/users/stats` - 获取用户统计

### 成就系统
- `GET /api/achievements` - 获取所有成就
- `GET /api/achievements/user` - 获取用户成就

## 🎯 功能特色

### 1. 智能打卡提醒
系统会根据用户设置的习惯频率，智能提醒用户完成打卡。

### 2. 连续打卡奖励
连续打卡可获得额外积分奖励，激励用户坚持习惯。

### 3. 成就系统
解锁各种成就徽章，增加趣味性和成就感。

### 4. 社交竞争
通过排行榜与其他用户竞争，提升参与度。

### 5. 数据可视化
直观的图表展示习惯完成情况和进步趋势。

## 📄 许可证

本项目采用 MIT 许可证 

---

⭐ 如果这个项目对你有帮助，请给一个 Star！
